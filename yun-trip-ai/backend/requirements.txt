# =============================================================================
# 云途 AI 行程规划 - Python 依赖列表
# =============================================================================
# 安装命令：pip install -r requirements.txt
# 建议在虚拟环境中安装，避免与系统包冲突。
# =============================================================================

# -------------------- Web 框架 --------------------
fastapi>=0.110,<1.0          # FastAPI Web 框架
uvicorn>=0.29,<1.0           # ASGI 服务器（生产环境可替换为 gunicorn + uvicorn worker）
pydantic>=2.6,<3.0           # 数据校验与序列化

# -------------------- 数据库 --------------------
sqlalchemy>=2.0,<3.0         # ORM 框架，当前使用 SQLite

# -------------------- 配置管理 --------------------
python-dotenv>=1.0,<2.0      # 从 .env 文件加载环境变量

# -------------------- 大模型 / LangChain 生态 --------------------
langchain>=0.2,<0.4           # LangChain 核心
langchain-core>=0.2,<0.4      # LangChain 基础抽象
langchain-community>=0.2,<0.4 # LangChain 社区集成（向量库等）
langchain-openai>=0.1,<0.3    # LangChain OpenAI 兼容适配器

# -------------------- 向量数据库 --------------------
chromadb>=0.5,<1.0           # Chroma 向量数据库（本地持久化）
pymilvus>=2.4,<3.0           # Milvus Lite 向量数据库（可选，通过 VECTOR_STORE 切换）

# -------------------- HTTP 客户端 --------------------
httpx>=0.27,<1.0              # 异步 HTTP 客户端（高德 API、Rerank API 调用）

# -------------------- 缓存 --------------------
redis>=5.0,<6.0               # Redis 客户端（可选：缓存 LLM 结果、天气、地图等）

# -------------------- 鉴权 --------------------
python-jose[cryptography]>=3.3,<4.0  # JWT 生成与验证
bcrypt>=4.0,<5.0                      # 密码哈希（bcrypt 算法，直接调用）

# -------------------- 导出 --------------------
reportlab>=4.0,<5.0           # PDF 生成库

# -------------------- 开发 / 测试 --------------------
pytest>=8.0,<9.0              # 测试框架